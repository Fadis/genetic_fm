#ifndef WAV2IMAGE_GET_IMAGE_DISTANCE_H
#define WAV2IMAGE_GET_IMAGE_DISTANCE_H

#include <cstdint>
#include <vector>

double get_image_distance( const std::vector< uint8_t > &l, const std::vector< uint8_t > &r );

#endif

